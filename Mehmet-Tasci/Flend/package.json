{
    "name": "flend-root",
    "scripts": {
        "install": "cd frontend && npm install && cd ../api && npm install",
        "build": "cd frontend && npm run build"
    },
    "dependencies": {
        "uuid": "^9.0.1"
    }
}
